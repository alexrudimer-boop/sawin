# Kernel corridor audit

Date: 2026-05-28

This generated audit scans local-minimal congruence-cover intervals and
records the least admissible congruence family generated by coordinate
kernels.  It is a finite stress test only.  It does not prove the
Master Local-Minimal Residual Theorem.

The output-kernel closure uses the two nondegeneracy coordinate kernels
`y -> pr_1 T(x,y)` and `x -> pr_2 T(x,y)`.  The all-coordinate closure
also includes the coretraction-side kernels `x -> pr_1 T(x,y)` and
`y -> pr_2 T(x,y)`.  The stable depth is `0` exactly when the seed
kernel graph already has the final connected components before any
transport through local tables or inverse local tables adds a new
edge.

## size_2_exhaustive

Local-minimal covers: `5`.
Semisplit leaks among local-minimal covers: `0`.

Output closure kinds: `equality`: `4`, `universal`: `1`.
All-coordinate closure kinds: `universal`: `5`.
Output universal depths: `0`: `1`.
Bi-free output closure kinds: `{}`.
Bi-free universal depths: `{}`.

Product-side witness counts:

- `retraction=equality|coretraction=universal|product=False|direct=True`: `1`
- `retraction=universal|coretraction=equality|product=True|direct=False`: `4`

Corridor rows:

- retraction `equality`, coretraction `universal`, tags `involutive+identity_table`, output `universal` depth `0` from `2` seeds, all-coordinate `universal` depth `0` from `2` seeds, product witness `False`, direct witness `True`: `1`.
- retraction `universal`, coretraction `equality`, tags `involutive+permutation_form+left_nondegenerate+right_nondegenerate+nondegenerate`, output `equality` depth `0` from `0` seeds, all-coordinate `universal` depth `0` from `2` seeds, product witness `True`, direct witness `False`: `1`.
- retraction `universal`, coretraction `equality`, tags `permutation_form+left_nondegenerate+right_nondegenerate+nondegenerate`, output `equality` depth `0` from `0` seeds, all-coordinate `universal` depth `0` from `2` seeds, product witness `True`, direct witness `False`: `1`.
- retraction `universal`, coretraction `equality`, tags `rack_type+involutive+permutation_form+left_nondegenerate+right_nondegenerate+nondegenerate`, output `equality` depth `0` from `0` seeds, all-coordinate `universal` depth `0` from `2` seeds, product witness `True`, direct witness `False`: `1`.
- retraction `universal`, coretraction `equality`, tags `rack_type+permutation_form+left_nondegenerate+right_nondegenerate+nondegenerate`, output `equality` depth `0` from `0` seeds, all-coordinate `universal` depth `0` from `2` seeds, product witness `True`, direct witness `False`: `1`.

## size_3_exhaustive

Local-minimal covers: `134`.
Semisplit leaks among local-minimal covers: `0`.

Output closure kinds: `equality`: `122`, `universal`: `12`.
All-coordinate closure kinds: `universal`: `134`.
Output universal depths: `0`: `12`.
Bi-free output closure kinds: `equality`: `12`, `universal`: `6`.
Bi-free universal depths: `0`: `6`.

Product-side witness counts:

- `retraction=equality|coretraction=universal|product=False|direct=True`: `6`
- `retraction=universal|coretraction=equality|product=True|direct=False`: `110`

Corridor rows:

- retraction `equality`, coretraction `equality`, tags `involutive`, output `universal` depth `0` from `2` seeds, all-coordinate `universal` depth `0` from `2` seeds, product witness `False`, direct witness `False`: `6`.
- retraction `equality`, coretraction `equality`, tags `left_nondegenerate+right_nondegenerate+nondegenerate`, output `equality` depth `0` from `0` seeds, all-coordinate `universal` depth `0` from `6` seeds, product witness `False`, direct witness `False`: `11`.
- retraction `equality`, coretraction `equality`, tags `rack_type+left_nondegenerate+right_nondegenerate+nondegenerate`, output `equality` depth `0` from `0` seeds, all-coordinate `universal` depth `0` from `6` seeds, product witness `False`, direct witness `False`: `1`.
- retraction `equality`, coretraction `universal`, tags `involutive+identity_table`, output `universal` depth `0` from `2` seeds, all-coordinate `universal` depth `0` from `2` seeds, product witness `False`, direct witness `True`: `6`.
- retraction `universal`, coretraction `equality`, tags `involutive+left_nondegenerate+right_nondegenerate+nondegenerate`, output `equality` depth `0` from `0` seeds, all-coordinate `universal` depth `0` from `2` seeds, product witness `True`, direct witness `False`: `6`.
- retraction `universal`, coretraction `equality`, tags `involutive+permutation_form+left_nondegenerate+right_nondegenerate+nondegenerate`, output `equality` depth `0` from `0` seeds, all-coordinate `universal` depth `0` from `2` seeds, product witness `True`, direct witness `False`: `3`.
- retraction `universal`, coretraction `equality`, tags `involutive+permutation_form+left_nondegenerate+right_nondegenerate+nondegenerate`, output `equality` depth `0` from `0` seeds, all-coordinate `universal` depth `0` from `6` seeds, product witness `True`, direct witness `False`: `2`.
- retraction `universal`, coretraction `equality`, tags `left_nondegenerate+right_nondegenerate+nondegenerate`, output `equality` depth `0` from `0` seeds, all-coordinate `universal` depth `0` from `2` seeds, product witness `True`, direct witness `False`: `24`.
- retraction `universal`, coretraction `equality`, tags `permutation_form+left_nondegenerate+right_nondegenerate+nondegenerate`, output `equality` depth `0` from `0` seeds, all-coordinate `universal` depth `0` from `2` seeds, product witness `True`, direct witness `False`: `3`.
- retraction `universal`, coretraction `equality`, tags `permutation_form+left_nondegenerate+right_nondegenerate+nondegenerate`, output `equality` depth `0` from `0` seeds, all-coordinate `universal` depth `0` from `6` seeds, product witness `True`, direct witness `False`: `4`.
- retraction `universal`, coretraction `equality`, tags `rack_type+involutive+permutation_form+left_nondegenerate+right_nondegenerate+nondegenerate`, output `equality` depth `0` from `0` seeds, all-coordinate `universal` depth `0` from `2` seeds, product witness `True`, direct witness `False`: `57`.
- retraction `universal`, coretraction `equality`, tags `rack_type+left_nondegenerate+right_nondegenerate+nondegenerate`, output `equality` depth `0` from `0` seeds, all-coordinate `universal` depth `0` from `2` seeds, product witness `True`, direct witness `False`: `6`.
- retraction `universal`, coretraction `equality`, tags `rack_type+permutation_form+left_nondegenerate+right_nondegenerate+nondegenerate`, output `equality` depth `0` from `0` seeds, all-coordinate `universal` depth `0` from `2` seeds, product witness `True`, direct witness `False`: `3`.
- retraction `universal`, coretraction `equality`, tags `rack_type+permutation_form+left_nondegenerate+right_nondegenerate+nondegenerate`, output `equality` depth `0` from `0` seeds, all-coordinate `universal` depth `0` from `6` seeds, product witness `True`, direct witness `False`: `2`.

## Consequence

The audit has two roles.  First, it checks that semisplit admissible
families do not leak into the tiny local-minimal cover corpus.  This
is only finite evidence; the theorem still needs the symbolic
local-minimality argument.

Second, it distinguishes seed-only universal corridors from corridors
that require transported edges.  In the audited size-2 and size-3
local-minimal cover corpus, every output-kernel universal closure has
stable depth `0`.  On the bi-free side, the only universal output
corridors are the already tagged involutive rows.  This does not close
the universal coordinate-kernel branch, but it makes the next lemma
more precise: a proof should first handle seed-only universal
coordinate-kernel corridors symbolically, while a counterexample
search should look for a transported-depth corridor or a seed-only
corridor with residual holonomy outside the known measurable
branches.
