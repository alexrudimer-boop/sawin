# Size 5/6 Everywhere-Singular Core Search Prompt

Status: partially_answered / GPT-5.5 Pro supplied the U,V exact-cover search
pipeline, stronger everywhere-singular filters, and compressed rack-prefix
pressure method on 2026-06-04.

Prompt:

```text
Please answer self-containedly. We are working on Sawin's MathOverflow problem:

Does every finite bijective set-theoretic Yang-Baxter solution X admit domination by a finite rack Y, meaning

    ker rho^Y_n <= ker rho^X_n

for every braid group B_n?

Current endpoint and filters:

1. Known positive branches:
   - left/right nondegenerate via derived/guitar rack;
   - involutive via the two-point flip rack;
   - flip-across/twisted unions via product racks;
   - total quotient and active-factor certificates when proper equivariant factors reconstruct X^n injectively in all arities;
   - periodic observer-rack factorization.

2. Fixed-arity rack cofinality is correct: for each fixed n, every finite quotient representation of B_n is dominated at that arity by some finite conjugation rack. Therefore a true no-rack counterexample must be asymptotically rack-invisible:

       forall m forall N exists n>N with N_{m,n}(X) != 1.

3. A minimal counterexample outside one-sided nondegenerate branches and without proper crossing-closed subsolutions must be everywhere singular:

       every L_x and every R_x is non-bijective

   where r(x,y)=(L_x(y),R_y(x)).

4. Periodic observer-rack factorization theorem:

   If finite periodic left-to-right transducers output a rack word in Y^n and invariant observer word in I^n, satisfy the local two-letter equivariance/invariance equations, and jointly inject X^n in every arity, then ker rho^Y_n <= ker rho^X_n for every n. If the Y-output is surjective in every arity, the kernels are equal.

   This covers:
   - X={0,1} x S_3 with r((e,g),(f,h))=((e,ghg^{-1}),(f,g)), using the S_3 conjugation rack plus inert observer bits;
   - the affine F_2^3 tetrahedral example, using period 3.

5. Minimal-image filter:

   For an everywhere singular table define

       S_L = union of im L_x over x with |im L_x| minimal,
       S_R = union of im R_x over x with |im R_x| minimal.

   If either S_L or S_R is a nonempty proper crossing-closed subset, then X is not a rigid core. A serious rigid-core candidate must pass this filter:

       S_L=X or r(S_L^2) != S_L^2,
       S_R=X or r(S_R^2) != S_R^2.

The next finite frontier proposed is:

    |X|=5 and |X|=6 everywhere-singular rigid-core enumeration,
    then P_{<=4} rack-prefix pressure through n<=7.

The rigid-core filters to impose are:

1. r:X^2 -> X^2 is a bijection and satisfies YBE.
2. Every L_x and every R_x is non-bijective.
3. r^2 != id.
4. No nonempty proper crossing-closed subsolution S with r(S^2)=S^2.
5. No nontrivial total YBE quotient congruence.
6. No nonconstant one-state invariant observer.
7. No flip-across/twisted-union partition.
8. Minimal-image filter passes on both left and right.
9. No obvious transport/monodromy rackification or proper active-factor certificate.

After those filters, compute pressure against

    P_{<=4} = product of one representative of every finite rack of size <=4

by checking, for 2<=n<=7,

    Gamma_{<=4,n}(X)
      = < (rho^{P_{<=4}}_n(sigma_i), rho^X_n(sigma_i)) : 1<=i<n >

and

    N_{<=4,n}(X) = { g_X : (1,g_X) in Gamma_{<=4,n}(X) }.

Task:

Give a concrete enumeration/search strategy for |X|=5 and |X|=6 that is realistic and mathematically correct. I need more than "brute force all bijections of X^2".

Please provide one of the following:

A. A proof that no |X|=5 everywhere-singular rigid-core candidate exists, using finite-table/YBE identities rather than full brute force.

B. A constructive normal form or backtracking scheme for |X|=5,6 that enforces bijectivity, everywhere singularity, and YBE early. Include the exact pruning invariants and canonicalization under relabeling.

C. A smaller theoretical filter stronger than the minimal-image filter, forced by everywhere singularity, that should be implemented before rack-prefix pressure. Examples: image-hypergraph closure, Green-relation/Rees decomposition, rank profiles, congruence generated by image fibres, or semigroup holonomy.

D. An explicit finite table of size 5 or 6 satisfying all rigid-core filters above, or a proof that any such table is already covered by periodic observer-rack factorization or another known branch.

E. If rack-prefix pressure through P_{<=4}, n<=7 is infeasible as stated, give the precise compressed group/matrix/permutation method that makes it feasible, or explain the smallest feasible replacement that still has mathematical value.

Be explicit enough that the answer can be converted into code/tests. Avoid broad roadmaps unless they include concrete invariants, data structures, and stopping criteria.
```
