# Rigid Pressure Core Search

Status: queued for GPT-5.5 Pro on 2026-06-04.

Prompt:

```text
Continue in the same chat. Please focus on the next falsifiable finite-table target for Sawin's finite rack domination problem.

Problem. For a finite bijective set-theoretic Yang-Baxter solution X, Sawin asks whether there is always a finite rack Y such that

    ker(B_n acting on Y^n) <= ker(B_n acting on X^n)

for every n.

Known positive branches: left- or right-nondegenerate solutions via the guitar/derived-rack theorem; involutive solutions via the two-point flip rack; flip-across/twisted unions of already dominated components; small size-4 degenerate non-involutive examples that are braid-equivalent to small racks; and affine hidden cyclic rows with explicit finite sequential rack gauges plus observers.

Known certificate theorem. If X admits finitely many total B_n-equivariant sequential active factors into finite racks or strictly smaller already dominated finite YBE solutions, plus invariant observer channels, and the combined output is injective on X^n for every n, then X is rack-dominated. This is only a certificate theorem. No universal proper active-factor observability theorem is known.

The exact negative condition is the cofinal rack-prefix obstruction. Enumerate finite racks Y_1,Y_2,... and set

    P_m = Y_1 x ... x Y_m.

For fixed m,n define

    Gamma_{m,n}(X)
      =
    < (rho^{P_m}_n(sigma_i), rho^X_n(sigma_i)) : 1 <= i < n >
    <= Sym(P_m^n) x Sym(X^n)

and

    N_{m,n}(X) = { g_X : (1,g_X) in Gamma_{m,n}(X) }.

Sawin-negative is exactly:

    forall m exists n, N_{m,n}(X) != 1.

The next finite-table target suggested by the previous answer is a rigid pressure core. Call a finite bijective YBE solution X a rigid pressure core if it satisfies all of:

1. Terminal filters: X is left-degenerate and right-degenerate, r_X^2 != id, not rack-type, not a flip-across union of smaller pieces, and not kernel-equivalent in the first few arities to any rack of size <= 3.
2. Quotient-rigid: every YBE congruence on X is equality or universal.
3. Subsolution-rigid: there is no nonempty proper subset S with r_X(S^2)=S^2.
4. Observer-rigid: the coarsest one-state invariant observer partition is universal; equivalently the graph generated by x~u and y~v for r(x,y)=(u,v) is connected.
5. No transport-isomorphic fibre splitting: no proper congruence or crossing-closed partition gives product-like mixed rows with transports that are isomorphisms of the internal block solutions and with rack-dominated quotient/blocks.
6. Real small-rack pressure: for the first rack prefix containing all racks of size <= 3, there is some manageable n with N_{m,n}(X) != 1.

Executable finite checks now show:

- Exhaustively, all 73 size-3 bijective YBE tables fail before this target: 66 fail bidegeneracy and the remaining 7 fail noninvolutivity.
- The size-4 affine Type A representative fails quotient-rigidity, subsolution-rigidity, and observer-rigidity.
- The size-4 Type B flip-across representative fails the flip-across exclusion and has transport-isomorphic split structure.
- The affine F_2^3 hidden cyclic pressure row fails observer-rigidity and subsolution-rigidity, and is closed by a sequential cyclic rack gauge plus inert observer.

Question. Can you do one of the following decisively?

A. Give a concrete finite bijective YBE table X, preferably of size 5 or a small structured affine-linear family, satisfying conditions 1--6 above. If possible, also test whether its rack-prefix pressure persists cofinally.

B. Prove that rigid pressure cores cannot exist, by showing that any finite bijective YBE solution surviving the terminal filters must have a proper quotient, proper subsolution, nontrivial invariant observer, flip-across decomposition, transport-split factor, sequential rack gauge, or finite rack prefix that already dominates it.

C. Sharpen the rigid pressure core definition by removing redundant filters or replacing condition 5 with a fully finite, mechanically checkable condition that still blocks all known positive mechanisms.

D. If neither a proof nor a table is available, propose the smallest concrete search space and exact pruning strategy most likely to find a rigid pressure core or prove none exists at that size. The answer should include enough table equations/normal forms for an implementation, not just high-level advice.

Please do not answer with only bounded search failure or a new certificate theorem. The goal is a concrete table, a proof of impossibility, or a sharply implementable next search.
```
