# Profinite Stabilizer Separation Prompt

Status: answered on 2026-06-05.

Follow-up status:

GPT-5.5 Pro confirmed the profinite stabilizer criterion, but found a sharper
structural correction: the relation `(VL1)` collapses every prefix generator
`a_{p,x}` to `a_{1,p(x)}`.  Thus the prefix vertex rack `V_L(X)` is really the
normalized prefix rack `W_L(X)` on generators `b_z`, `z in X`, with relations

    b_{pL_u(v)} = b_{p(u)} triangleright b_{p(x)}.

Consequently, prefix vertexization through `V_L(X)` factors through the
normalized prefix map

    D_n(x_1,...,x_n)=(x_1,L_{x_1}(x_2),...,L_{x_1}...L_{x_{n-1}}(x_n)).

The residual route now has two independent barriers: normalized-prefix
orbit-faithfulness and profinite stabilizer separability in
`As(W_L(X))`.  The next active prompt asks whether this normalized-prefix
barrier can be bypassed by a full-arrow rack detector, converted into an
active factor, or proved harmless for residual rigid cores.

Prompt:

```text
Please answer self-containedly and mathematically. Do not give only a bounded
computation. The goal is to decide whether the profinite stabilizer-separation
lemma can close the remaining residual branch of Sawin's finite
rack-domination problem, or whether it exposes a genuine obstruction.

Sawin asks: for every finite bijective set-theoretic Yang-Baxter solution X,
does there exist a finite rack Y such that

    ker rho_n^Y <= ker rho_n^X      for every n?

Write

    r_X(x,y)=(L_x(y),R_y(x)).

Known reductions and guardrails:

1. Product observers are genuine inert braid observers but are kernel-universal
   by singular padding: for X=E x Z with E a nontrivial identity solution,
   ker rho_n^X=ker rho_n^Z.

2. Product-Hurwitz separability is a valid sufficient theorem, but finite
   residual separation of the suffix Hurwitz racks U_L(X), U_R(X) is an extra
   hypothesis.

3. Prefix-path rack vertexization is a valid sufficient theorem.  Let
   S=S_L^1 and define V_L(X) as the rack generated by

       a_{p,x}      (p in S, x in X)

   with relations, for r_X(x,y)=(u,v),

       a_{p,u} = a_{pL_x,y},                              (VL1)
       a_{pL_u,v} = a_{pL_x,y} triangleright a_{p,x}.     (VL2)

   A finite quotient of V_L(X) whose prefix labels separate all distinct points
   inside braid orbits gives a finite rack Y with

       ker rho_n^Y <= ker rho_n^X

   for all n.

4. Active factors must be domination-reducing.  A proper active factor Z must
   satisfy ker rho_n^Z <= ker rho_n^X, or this must follow from equivariant
   maps Pi_n:X^n -> Z^n and inert maps eta_n such that (Pi_n,eta_n) is
   injective.  Ordinary YBE quotients usually give the opposite containment.

Current exact obstruction.

Let R=V_L(X) and G=As(R).  For a prefix generator alpha=(p,x), let
bar alpha in R be its element and

    H_alpha = Stab_G(bar alpha).

Finite-rack residual equivalence on prefix vertices is:

    alpha ==_fin beta

iff every homomorphism R -> finite rack identifies alpha and beta.

If beta = g.alpha in the same G-orbit, then

    alpha ==_fin beta
    iff
    g in closure_prof(H_alpha)
    = intersection_{N normal finite index in G} H_alpha N.

Thus a genuine obstruction is an orbit-relevant nonclosed stabilizer coset:

    g notin H_alpha
    but
    g in closure_prof(H_alpha).

The residual branch closes if:

    For every residual rigid X with no proper domination-reducing active
    factor, no orbit-relevant bad holonomy coset occurs.

Equivalently, the relevant stabilizers in As(V_L(X)) are profinitely closed on
the cosets needed to separate braid-orbit points, unless the collapse yields a
proper active factor.

Task.

1. Audit the profinite criterion carefully.  Is finite-rack residual
   equivalence for V_L(X) exactly stabilizer coset closure in As(V_L(X))?
   Are there hidden assumptions about finite generation, orbit finiteness, or
   augmented rack realizability of the coset quotient?

2. Try to prove the stabilizer-separation lemma for residual rigid finite YBE
   solutions.  What special properties do the groups As(V_L(X)) have?  Are
   they residually finite, subgroup separable for relevant stabilizers, or
   virtually in a known separable class?

3. If the lemma is false, give the sharpest possible obstruction.  Ideally
   show one of:
   - a construction realizing a nonseparable subgroup/coset as a stabilizer
     coset in As(V_L(X)) for some finite bijective YBE solution X;
   - a semigroup/Rees matrix pattern whose prefix holonomy rackification has a
     nonclosed orbit-relevant stabilizer coset;
   - or a proof that arbitrary finitely presented group separability problems
     can be embedded into As(V_L(X)), making the route too strong.

4. Determine whether a nonclosed orbit-relevant stabilizer coset necessarily
   yields a proper domination-reducing active factor.  If yes, prove the
   construction of Z, Pi_n, and eta_n.  If no, isolate exactly why the
   prefix-dependent collapse cannot be turned into an active factor.

5. Look for a smaller sufficient theorem.  For example:
   - conditions on V_L(X) implying relevant stabilizers are separable;
   - a finite quotient phi:As(V_L(X))->Gbar whose coset rack separates all
     orbit-relevant prefix vertices;
   - or a finite rack quotient psi:S_L^1 x X -> Y satisfying (V1),(V2) with
     x -> psi(p,x) injective for every p.

6. Decide whether this route can plausibly finish Sawin, or whether the
   profinite stabilizer obstruction should be treated as a new independent
   barrier.

Important:

- Do not claim product observers alone prove domination.
- Do not use ordinary quotients as active factors unless the kernel-reflecting
  direction is proved.
- Do not replace profinite subgroup separability with finite semigroup closure.
- Do not identify V_L(X) with U_L(X) without proving the required opposite or
  reversal symmetry.
- A useful answer should either prove the stabilizer-separation lemma, refute
  it with a realizable obstruction, or reduce it to a smaller checkable
  structural theorem.
```
