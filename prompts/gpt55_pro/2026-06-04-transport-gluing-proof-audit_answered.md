# Transport-Isomorphic Gluing Proof Audit

Status: answered by GPT-5.5 Pro on 2026-06-04 via the transport-gluing counteraudit.

Prompt:

```text
Continue in the same chat. Please audit the following proof decisively. Assume no repo access; all definitions are included here.

Let X be a finite bijective set-theoretic Yang-Baxter solution. A finite rack Y dominates X if

    ker(B_n acting on Y^n) <= ker(B_n acting on X^n)

for every n.

Let theta be a proper YBE congruence on X, with quotient Z and crossing-closed fibres F_a over a in Z. Assume Z is rack-dominated and every internal block solution F_a is rack-dominated.

For distinct quotient blocks a,b, write r_Z(a,b)=(c,d). The mixed row

    r_X : F_a x F_b -> F_c x F_d

is product-like if it is either direct:

    r_X(x,y)=(phi_{a,b}(x), psi_{a,b}(y))

or swapped:

    r_X(x,y)=(phi_{a,b}(y), psi_{a,b}(x)).

Assume every mixed row is product-like and every one-variable transport map phi,psi is an isomorphism of the relevant internal block YBE solutions.

A previous answer claimed this proves X is rack-dominated by:

1. forming the finite transport groupoid generated by these block isomorphisms;
2. choosing representative fibres and gauges kappa_a:F_a -> F_bar(a);
3. replacing every mixed transport by identity to obtain a split solution T;
4. proving T is dominated by the quotient rack and the internal block racks;
5. constructing a finite sequential gauge F_n:X^n -> T^n that is injective and B_n-equivariant.

I need you to audit the proof, not just restate it.

Potential gap 1: transport-isomorphic edge maps need not be simultaneously gauge-normalizable to identity unless the transport groupoid has trivial loop monodromy. YBE coherence seems to make a functorial groupoid action, but coherent loop automorphisms can still be nontrivial. Does the hypothesis above force trivial loop monodromy? If yes, prove it. If not, repair the theorem by incorporating the finite loop monodromy into the detector/transducer, or give a counterexample to the identity-normalization step.

Here is a concrete witness that the flatness issue is real. Let

    X = {0,1} x Z/3,
    r((a,x),(b,y)) = ((a,y),(b,x+1)).

The quotient by the first coordinate is the identity solution on two colours; the fibres over 0 and 1 are crossing-closed three-point subsolutions. All mixed rows are swapped-product-like. The one-variable transports are internal-solution isomorphisms, but the transport groupoid has loop monodromy of order 3 on each fibre: moving around a colour loop applies x -> x+1. Thus transport-isomorphic product-like rows do not force simultaneous identity gauge-normalization. This example is not a Sawin counterexample; the local product router treats it as an identity-base cyclic row, closes it by a finite cyclic detector C_3, and assigns verdict product_finite_g_branch with no detector gaps. Its role is to kill the naive identity-normalization step and force the repaired proof to include finite monodromy detectors explicitly.

Potential gap 2: the proof that the split solution T is dominated by quotient and block racks says that, after quotient triviality, one can delete all strands not carrying colour a and get an internal braid beta_a. This is clear for flip-across or permutation-like colour motion, but a general finite quotient YBE solution can change colours along the braid trajectory and return only at the end. Does the product-like transport-isomorphic hypothesis force enough strand-colour conservation for this substrand deletion argument? If yes, prove it. If not, give the correct finite marked-factor or active-factor certificate needed for T.

Please give one of the following outcomes:

A. a rigorous repaired proof that transport-isomorphic product-like gluing really implies rack-domination under exactly the stated hypotheses, explicitly handling nontrivial transport loop monodromy and non-permutation quotient colour dynamics; or

B. a precise strengthened theorem with the missing extra hypotheses, such as trivial transport loop monodromy, permutation/flip-across quotient colour routing, or an explicit finite active-factor certificate; or

C. a concrete finite YBE table satisfying the stated hypotheses where the claimed split-gauge/product-rack proof fails.

The answer should be decisive about whether the original Question A is actually proved as stated or whether it remains only a certificate theorem pending the split-tower domination lemma.
```
