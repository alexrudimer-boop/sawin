theoretical_answered

Status: answered by the response reviewed in
`proofs/2026_06_06_theoretical_chief_factor_response_review.md`.  The valid
progress was recorded in
`proofs/chief_factor_finite_image_obstruction.md`.  The current active prompt
is `prompts/gpt55_pro/2026-06-06-theoretical_asknow.md`.

This is a self-contained theoretical prompt.  Do not ask for code,
repository access, or further computations.  Be aggressive: try to fully
resolve Will Sawin's problem from the finite-image symmetric-commutator
boundary below.

Problem, due to Will Sawin.

Let X be a finite bijective set-theoretic solution of the Yang-Baxter
equation, with braid actions

  rho^X_n : B_n -> Sym(X^n).

A finite rack Y is viewed as the set-theoretic solution

  R_Y(a,b) = (a*b, a).

Question.  Does every finite bijective set-theoretic YBE solution X admit a
single finite rack Y, independent of n, such that

  ker rho^Y_n <= ker rho^X_n

for every n?

Known closed cases and guardrails.

1. Finite one-sided nondegenerate solutions are closed by the derived
   rack/guitar construction, with kernel equality in every arity.

2. Involutive solutions, products of dominated solutions, and braided
   quotients of dominated solutions are dominated.

3. No single braid is invisible to all finite racks, and fixed-arity rack
   cofinality is available.  Any counterexample must force failures at
   unbounded braid index.

4. The ordinary Brunnian reduction is known.  For a rack Y, put
   Q=Y^0 x T_2, where Y^0 is the transparent extension and T_2 is the
   two-element trivial rack.  Then Q dominates X if and only if

   Brun_n cap ker rho^Q_n <= ker rho^X_n

   for every n, where Brun_n is the ordinary Brunnian subgroup of P_n.

Finite-image symmetric-commutator boundary.

Fix a finite rack detector Y and set Q=Y^0 x T_2.  Let

  F_{n-1}=ker(d_n:P_n -> P_{n-1})

be the last-strand free group with free generators

  x_i = A_{in},       1 <= i <= n-1.

Let

  Phi_n:F_{n-1} -> Gamma_n <= Sym(Q^n) x Sym(X^n)

be the combined finite image

  Phi_n(beta) = (rho^Q_n(beta), rho^X_n(beta)).

Let p_Q and p_X be the two projections from Gamma_n, and set

  K_n = ker p_Q,
  L_n = ker p_X.

For 1<=i<=n-1, set

  N_{i,n} = << Phi_n(x_i) >>_{Gamma_n}.

For normal subgroups N_1,...,N_r of a group G, write

  [N_1,...,N_r]_Sigma

for the symmetric commutator subgroup generated by all fully parenthesized
iterated commutators with one input from each N_i, in arbitrary order.

Known theorem:

  Phi_n(Brun_n cap ker rho^Q_n)
    =
  K_n cap [N_{1,n},...,N_{n-1,n}]_Sigma.

Therefore Q dominates X if and only if

  K_n cap [N_{1,n},...,N_{n-1,n}]_Sigma <= L_n

for every n.

The free-group input is the projection-kernel symmetric commutator theorem:
in F=<x_1,...,x_r>, the intersection of the kernels of the projections
killing individual x_i equals the all-variable symmetric commutator of the
normal closures <<x_i>>.

Useful sufficient criteria.

Relative lower-central criterion.  If there is a constant c such that, for
every n>c+1,

  K_n cap gamma_{c+1}(Gamma_n) <= L_n,

then X is dominated by one finite rack.  The finitely many arities n<=c+1 are
handled by fixed-arity rack cofinality.

Redundant-meridian criterion.  If, for all sufficiently large n, in
Gamma_n/L_n one of the images of N_{i,n} centralizes the subgroup generated by
all the other meridian normal closures, then the all-meridian symmetric
commutator vanishes modulo L_n.  After adding finitely many fixed-arity
detectors, X is dominated by one finite rack.

Your task.

Try to resolve the full problem from this finite-image boundary.

A. Prove Sawin's statement for every finite bijective degenerate X.  A clean
   route would be to construct a finite rack Y such that the finite-image
   containment

     K_n cap [N_{1,n},...,N_{n-1,n}]_Sigma <= L_n

   holds for every n.  You may instead prove the relative lower-central
   criterion, the redundant-meridian criterion, or a stronger finite-image
   theorem for every finite X.

B. Give an explicit finite degenerate bijective YBE table X and prove no
   finite rack dominates it by constructing the cofinal finite-image
   symmetric-commutator obstruction: for every finite rack prefix P_m, with
   Q_m=P_m^0 x T_2, find unbounded n_m for which

     K_{n_m} cap [N_{1,n_m},...,N_{n_m-1,n_m}]_Sigma not <= L_{n_m}.

   It is not enough to defeat one rack prefix, one detector product, bounded
   arity, or bounded deletion depth.

C. If neither A nor B can be proved, give only proof-grade progress: a
   theorem that strictly narrows the finite-image symmetric-commutator
   obstruction, with proof and the exact remaining implication.  Clearly
   separate theorem/proof from finite evidence, heuristic, and unsupported
   claims.

Avoid returning only the nondegenerate theorem, contextual equivariance,
ordinary-Brunnian reduction, or finite-image formula itself.  Those are now
known.  The target is the all-arity containment in the finite combined image,
or a cofinal finite-image counterexample.
