theoretical_answered

Status: answered by the response reviewed in
`proofs/2026_06_06_theoretical_pairwise_core_response_review.md`.  The valid
progress was recorded in
`proofs/pairwise_core_finite_image_obstruction.md`.  The current active prompt
is `prompts/gpt55_pro/2026-06-06-theoretical_asknow.md`.

This is a self-contained theoretical prompt.  Do not ask for code,
repository access, or further computations.  Be aggressive: try to fully
resolve Will Sawin's problem from the pairwise-or-central finite-image
boundary below.

Problem, due to Will Sawin.

Let X be a finite bijective set-theoretic solution of the Yang-Baxter
equation, with braid actions

  rho^X_n : B_n -> Sym(X^n).

A finite rack Y is viewed as the set-theoretic solution

  R_Y(a,b) = (a*b, a).

Question.  Does every finite bijective set-theoretic YBE solution X admit a
single finite rack Y, independent of n, such that

  ker rho^Y_n <= ker rho^X_n

for every n?

Known closed cases and guardrails.

1. Finite one-sided nondegenerate solutions are closed by the derived
   rack/guitar construction, with kernel equality in every arity.

2. The degenerate case is not a smaller residual problem.  If D is a finite
   identity solution with |D|>1 and Z is any finite bijective solution, then
   X=D x Z is finite, bijective, and degenerate, and

     ker rho^X_n = ker rho^Z_n

   for every n.  Therefore proving domination for all finite degenerate
   solutions is equivalent to proving the full problem.

3. Products of dominated solutions and braided quotients of dominated
   solutions are dominated.

4. No single braid is invisible to all finite racks, and fixed-arity rack
   cofinality is available.  Any counterexample must force failures at
   unbounded braid index against every finite rack prefix.

5. The ordinary Brunnian reduction is known.  For a rack Y, put
   Q=Y^0 x T_2, where Y^0 is the transparent extension and T_2 is the
   two-element trivial rack.  Then Q dominates X if and only if

     Brun_n cap ker rho^Q_n <= ker rho^X_n

   for every n, where Brun_n is the ordinary Brunnian subgroup of P_n.

Finite-image setup.

Fix a finite rack detector Y and set Q=Y^0 x T_2.  Let

  F_{n-1}=ker(d_n:P_n -> P_{n-1})

be the last-strand free group with free generators

  x_i = A_{in},       1 <= i <= n-1.

Let

  Phi_n:F_{n-1} -> Gamma_n <= Sym(Q^n) x Sym(X^n)

be the combined finite image

  Phi_n(beta) = (rho^Q_n(beta), rho^X_n(beta)).

Let p_Q and p_X be the two projections from Gamma_n, and set

  K_n = ker p_Q,
  L_n = ker p_X.

For 1<=i<=n-1, set

  N_{i,n} = << Phi_n(x_i) >>_{Gamma_n}.

For normal subgroups N_1,...,N_r of a group G, write

  [N_1,...,N_r]_Sigma

for the symmetric commutator subgroup generated by all fully parenthesized
iterated commutators with one input from each N_i, in arbitrary order.

Known finite-image theorem:

  Phi_n(Brun_n cap ker rho^Q_n)
    =
  K_n cap [N_{1,n},...,N_{n-1,n}]_Sigma.

Since Gamma_n is a subgroup of Sym(Q^n) x Sym(X^n), the projection kernels
K_n and L_n intersect trivially.  Therefore Q dominates X if and only if

  K_n cap [N_{1,n},...,N_{n-1,n}]_Sigma = 1

for every n.

Pairwise-or-central chief-factor boundary.

The normal closures N_{1,n},...,N_{n-1,n} generate Gamma_n.  The following
group-theoretic compression is known.

Let G be a finite group generated by normal subgroups N_1,...,N_r with r>=2,
let K be normal in G, and put C=[N_1,...,N_r]_Sigma.  If K cap C != 1, then
there is a chief factor A/B inside K with one of the following forms:

  Pairwise obstruction:
    [N_i,N_j] covers A/B for some i!=j.

  Central abelian obstruction:
    A/B is elementary abelian, A/B <= Z(G/B), and C covers A/B.

More sharply:

  If A/B is nonabelian, every pair [N_i,N_j] covers A/B.

  If A/B is abelian but noncentral, then some N_j acts nontrivially on A/B,
  and [N_i,N_j] covers A/B for every i!=j.

Therefore every finite-image failure for n>=3 is either:

  Type P. A pairwise obstruction

    K_n cap [N_{i,n},N_{j,n}] != 1

  for some i!=j, possibly after passing to a chief factor; or

  Type C. A central elementary abelian chief factor

    A/B <= K_n,
    A/B <= Z(Gamma_n/B),

  covered by the full all-meridian commutator

    [N_{1,n},...,N_{n-1,n}]_Sigma.

Useful conditional criterion.

Let Z be a braided quotient of X already dominated by a finite rack.  Let

  G^X_n = rho^X_n(F_{n-1}),
  G^Z_n = rho^Z_n(F_{n-1}),
  E_n = ker(G^X_n -> G^Z_n).

If

  E_n cap gamma_{n-1}(G^X_n)=1

for all sufficiently large n, then X is dominated by one finite rack.  It is
enough to have a uniform constant c such that

  E_n cap gamma_c(G^X_n)=1

for every n.  Use a detector Q=Y_Z^0 x T_2 containing a rack Y_Z that
dominates Z, and add finitely many fixed-arity detectors for small n.

Your task.

Try to resolve the full problem from this boundary.

A. Prove Sawin's statement for every finite bijective X.  A clean route would
   be to construct, or prove the existence of, a finite rack detector
   Q=Y^0 x T_2 such that for all sufficiently large n:

     K_n cap [N_{i,n},N_{j,n}] = 1      for every i!=j,

   and there is no central elementary abelian chief factor A/B inside K_n,
   central in Gamma_n/B, covered by the all-meridian commutator.

   You may instead prove the relative lower-central criterion for a suitable
   dominated quotient Z of every X, or any stronger all-arity theorem implying
   the same finite-image vanishing.

B. Give an explicit finite bijective YBE table X and prove no finite rack
   dominates it.  The proof must be cofinal: for every finite rack prefix
   P_m, with Q_m=P_m^0 x T_2, construct unbounded arities n_m producing
   either a Type P pairwise obstruction or a Type C central elementary
   abelian high-Brunnian obstruction inside the corresponding Gamma_{n_m}.
   It is not enough to defeat one rack prefix, one detector product, bounded
   arity, bounded deletion depth, or one Brunnian template without the
   cofinal chief-factor proof.

C. If neither A nor B can be proved, give only proof-grade progress: a
   theorem that strictly narrows the Type P or Type C obstruction, with proof
   and the exact remaining implication.  Clearly separate theorem/proof from
   finite evidence, heuristic, and unsupported claims.

Avoid returning only the nondegenerate theorem, contextual equivariance,
ordinary-Brunnian reduction, finite-image formula, chief-factor dichotomy, or
pairwise-or-central compression itself.  Those are now known.  The target is
to eliminate pairwise finite-image obstructions and central elementary
abelian high-Brunnian obstructions in all sufficiently large arities, or to
construct a cofinal counterexample of one of those two narrowed types.
